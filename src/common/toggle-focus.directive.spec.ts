import { ToggleFocusDirective } from './toggle-focus.directive';
 
describe('ToggleFocusDirective', () => {
  it('should create an instance', () => {
    // const directive = new ToggleFocusDirective();
    // expect(directive).toBeTruthy();
  });
});
