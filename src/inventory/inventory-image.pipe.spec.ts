import { InventoryImagePipe } from './inventory-image.pipe';

describe('InventoryImagePipe', () => {
  it('create an instance', () => {
    const pipe = new InventoryImagePipe();
    expect(pipe).toBeTruthy();
  });
});
